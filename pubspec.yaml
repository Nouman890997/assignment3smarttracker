name: assignment3smarttracker
description: "A new Flutter project for the SmartTracker application."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  provider: ^6.1.2
  http: ^1.2.2

  # GPS / LOCATION
  geolocator: ^13.0.1   # <-- latest & stable

  # CAMERA
  camera: ^0.11.0+1

  # GOOGLE MAPS
  google_maps_flutter: ^2.6.0

  # LOCAL STORAGE
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
